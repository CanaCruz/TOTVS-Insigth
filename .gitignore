node_modules/
dist/
build/
.cache/
.env*
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.tmp-*

# Dump bruto das reuniões (46 MB) — nunca versionar: é conteúdo de cliente.
data-source/

# Artefatos gerados por `pnpm data:build`.
# O índice público PODE ir pro repo (para o CI publicar o Pages).
# As transcrições NÃO.
public/data/transcricoes/
# Mantém o índice versionável:
# public/data/meetings-index.json  ← commitar quando a base mudar
