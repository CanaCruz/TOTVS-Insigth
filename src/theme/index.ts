/**
 * Módulo de tema: tokens estáticos + preferência claro/escuro.
 *
 * `import { GRADIENT_BRAND } from "@/theme"`
 * `import { useTheme } from "@/theme/ThemeContext"`
 */
export {
  GRADIENT_BRAND,
  GRADIENT_PANEL,
  GRADIENT_PROGRESS,
  SIDEBAR_WIDTH,
} from "./tokens";
